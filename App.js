import React, { Component } from 'react';
import { View } from 'react-native';

import MasterNavigator from './src/navigation';

export default class App extends Component {
  render() {
    return (
      <MasterNavigator />
    );
  }
}

